spring.application.name=UBAKA

# ==========================================
# HARDCODED REGIONAL SUPABASE DATABASE SETUP
# ==========================================
# Routed using direct port 5432 to bypass local network blocks
spring.datasource.url=jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.qsulbwlwungsnvfiyfub
spring.datasource.password=Amasomo@123!

# PostgreSQL Hibernate Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==========================================
# RETAINED WEB & CONSOLE CONFIGURATIONS
# ==========================================
spring.output.ansi.enabled=always

spring.security.user.name=admin
spring.security.user.password=admin123

# Mandatory for dynamic cloud port binding on Render
server.port=${PORT:8080}